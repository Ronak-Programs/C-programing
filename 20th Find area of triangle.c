#include<stdio.h>
int main()
{
    float l;
    printf("Enter the value of l : ");
    scanf("%f", &l);

    float h;
    printf("Enter the value of h : ");
    scanf("%f", &h);

    float area;
    printf("Area of triangle is : %f", 0.5*l*h);

    return 0;
}
